package my.program

fun main(args: Array<String>) {
    // Create a new read-only Map<Integer, String>
    val mymap = mapOf(Pair(1, "BMW Cars"), Pair(2, "Socks"), Pair(3, "Names") )
    println(mymap)
}